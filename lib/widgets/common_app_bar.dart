import 'package:flutter/material.dart';

class CommonApBar extends StatelessWidget {
  CommonApBar({super.key});

  @override
  Widget build(BuildContext context) {
    return AppBar(
      backgroundColor: Colors.grey[900],
    );
  }
}
